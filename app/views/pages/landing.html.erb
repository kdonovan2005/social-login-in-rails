<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>

  </head>
  <body>

    <% if current_user != nil %>
      <% if current_user.vote == true %>

        <% if flash[:notice] %>
          <div class="notice"><%= flash[:notice] %></div>
        <% end %>
        <br>
        Please cast your vote:

        <%= form_tag exhibits_path, :method => 'get' do %>
          <%= text_field_tag :search, params[:search], data: { autocomplete: autocomplete_exhibit_entry_code_exhibits_path, update_elements: {} } %>
          <div class="results" id="search-results"></div>
          <%= submit_tag "Vote" %>
        <% end %>

        <h2>Now, pick a number associated with the work you like best.</h2>


      <% else %>
        Thank you for casting your vote!
        Here are the top contenders: <br>

        <% @topexhibits.each do |entry| %>
          <%= entry.entry_code %> <br>
          <%= entry.category %> <br>
          <%= entry.entry_name %> <br>
          <%= entry.entrant_name %> <br>
          <%= entry.entrant_company %> <br>
          <%= entry.vote_count %> <br> <br>
        <% end %>

      <% end %>

    <% else %>

      How did you get here? Please <a href="/users/sign_in">SIGN IN</a>

    <% end %>

  </body>

</html>
